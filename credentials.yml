# =============================================================================
# Eco-Travel Advisor - Channel Credentials
# =============================================================================

# REST API (used by Streamlit frontend and direct API calls)
rest:

# Rasa Webchat (browser widget)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Rasa X / Enterprise (uncomment when using)
# rasa:
#   url: "http://localhost:5002/api"

# Facebook Messenger (uncomment and fill in to enable)
# facebook:
#   verify: "your_verify_token"
#   secret: "your_app_secret"
#   page-access-token: "your_page_access_token"

# Slack (uncomment and fill in to enable)
# slack:
#   slack_token: "your_slack_token"
#   slack_channel: "your_slack_channel"
#   slack_signing_secret: "your_signing_secret"
